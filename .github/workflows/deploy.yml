name: Build & Deploy Docker to GCP VMs

on:
  push:
    branches: [ main, master ]

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    strategy:
      matrix:
        vm_ip: [34.64.151.197, 34.64.215.144, 34.64.84.23]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v3

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Login to DockerHub
        uses: docker/login-action@v2
        with:
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: true
          tags: ${{ secrets.DOCKER_USERNAME }}/moonpull:latest

      - name: Set up SSH key
        uses: webfactory/ssh-agent@v0.5.4
        with:
          ssh-private-key: ${{ secrets.GCP_SSH_KEY }}

      - name: Add host to known_hosts
        run: ssh-keyscan -H ${{ matrix.vm_ip }} >> ~/.ssh/known_hosts

      - name: SSH into VM and deploy container
        run: |
          ssh jaegeon@${{ matrix.vm_ip }} << 'EOF'
            echo "🔁 기존 컨테이너 중지"
            docker stop moonpull-app || true
            docker rm moonpull-app || true

            echo "⬇️ 최신 이미지 pull"
            docker pull ${{ secrets.DOCKER_USERNAME }}/moonpull:latest

            echo "🚀 컨테이너 재실행"
            docker run -d \
              --name moonpull-app \
              -p 443:443 \
              -v /home/jaegeon/moonpull/eskey:/app/eskey \
              ${{ secrets.DOCKER_USERNAME }}/moonpull:latest

            echo "✅ 배포 완료: $HOSTNAME"
          EOF
